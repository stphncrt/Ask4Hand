import React from "react";
import styled from "styled-components";
const SearchSection = () => {
  return <StyledContainer> SearchSection </StyledContainer>;
};

export default SearchSection;

export const StyledContainer = styled.div`
  height: 100px;
  background-color: red;
`;
